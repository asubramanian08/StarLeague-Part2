#include <iostream>
using namespace std;
typedef pair<int, int> point;
struct board
{
    point start;
    point end;
};

int main(void)
{
    /*FILE *junk;
    junk = freopen("boards.in", "r", stdin);
    junk = freopen("boards.out", "w", stdout);*/
    int goal, numBoards;
    cin >> goal >> numBoards;

    return 0;
}